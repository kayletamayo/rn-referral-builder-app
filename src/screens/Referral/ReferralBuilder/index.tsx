import React from "react";
import { View, Text } from "react-native";

interface Props {}

const ReferralBuilder: React.FC<Props> = () => {
  return (
    <View style={{ flex: 1 }}>
      <Text>Referral Builder</Text>
    </View>
  );
};

export default ReferralBuilder;
